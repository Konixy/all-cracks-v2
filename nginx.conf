server {
  listen 80;
  listen [::]:80;
  server_name example_server.com;

  location / {
    allow all
    root /index.html;
    include /etc/nginx/mime.types;
    try_files $uri /index.html;
  }
  root /index.html
  index index.html
}